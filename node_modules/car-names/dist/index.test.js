"use strict";

var _chai = require("chai");

var _index = _interopRequireDefault(require("./index"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

describe('start test car-names module', function () {
  describe('all', function () {
    it('should be an array of strings', function () {
      (0, _chai.expect)(_index["default"].all).to.satisfy(isArrayOfStrings);

      function isArrayOfStrings(array) {
        return array.every(function (item) {
          return typeof item === 'string';
        });
      }
    });
    it('should contain one car named `Ferrari`', function () {
      (0, _chai.expect)(_index["default"].all).to.include('Ferrari');
    });
  });
  describe('random', function () {
    it('should return a random item from the carNames.all', function () {
      var randomCar = _index["default"].random();

      (0, _chai.expect)(_index["default"].all).to.include(randomCar);
    });
  });
});